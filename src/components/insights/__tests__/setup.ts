import { server } from '../../../test/mocks/server';
import '@testing-library/jest-dom';

// Component-specific setup
beforeEach(() => {
  // Reset component-specific state
});

// You can extend the global setup with component-specific needs
beforeAll(() => {
  // Any additional setup needed for insights tests
});

afterEach(() => {
  // Component-specific cleanup
});

afterAll(() => {
  // Component-specific teardown
});